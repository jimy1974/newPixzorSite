<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Text-to-Image Generator</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <!-- Navbar -->
  <nav class="bg-white shadow-md">
  <div class="container mx-auto px-4 py-2 flex justify-between items-center">
    <div class="text-xl font-bold">Text-to-Image Generator</div>
    <div>
      <% if (user) { %>
        <span class="mr-4">Welcome, <%= user.username %></span>
        <!-- Display user tokens if available -->
        <% if (user.tokens) { %>
          <span class="mr-4">Tokens: <%= user.tokens %></span>
        <% } %>
        <a href="/logout" class="text-blue-500 hover:text-blue-700">Logout</a>
      <% } else { %>
        <a href="#" id="login-link" class="text-blue-500 hover:text-blue-700 mr-4">Login</a>
        <a href="#" id="register-link" class="text-blue-500 hover:text-blue-700">Register</a>
      <% } %>
    </div>
  </div>
</nav>

    
  <div class="container mx-auto py-8">
    <header class="mb-6">
      <h1 class="text-3xl font-bold text-center">Text-to-Image Generator</h1>
    </header>

    <div class="grid grid-cols-12 gap-4">
      <div class="col-span-3">
        <div class="bg-white p-4 rounded-lg shadow-md">
          <h2 class="text-lg font-bold mb-2">Options</h2>

          <div class="mb-4">
            <label for="face-upload" class="block font-medium mb-1">Upload Face</label>
            <input type="file" id="face-upload" class="w-full bg-gray-200 rounded-lg py-2 px-3">
          </div>

          <div class="mb-4">
            <label for="style-select" class="block font-medium mb-1">Select Style</label>
            <select id="style-select" class="w-full bg-gray-200 rounded-lg py-2 px-3">
              <option value="">Choose a style</option>
              <option value="pixar">Pixar</option>
              <option value="anime">Anime</option>
              <option value="cartoon">Cartoon</option>
            </select>
          </div>

         
          <div class="mb-4">
            <label for="variables-input" class="block font-medium mb-1">Set Variables</label>
            
             <!-- Aspect Ratio (if you want to make it variable) -->
<input type="text" id="aspect-ratio" value="16:9" class="w-full bg-gray-200 rounded-lg py-2 px-3" placeholder="Aspect Ratio">

<!-- Guidance Scale (optional input) -->
<input type="number" id="guidance-scale" value="3.5" class="w-full bg-gray-200 rounded-lg py-2 px-3" placeholder="Guidance Scale">

<!-- Number of Inference Steps (optional input) -->
<input type="number" id="inference-steps" value="4" class="w-full bg-gray-200 rounded-lg py-2 px-3" placeholder="Inference Steps">

              
              
          </div>
        </div>
      </div>

      <div class="col-span-9">
        <div class="bg-white p-4 rounded-lg shadow-md">
          <div class="mb-4">
            <label for="prompt-input" class="block font-medium mb-1">Enter Prompt</label>
            <textarea id="prompt-input" class="w-full bg-gray-200 rounded-lg py-2 px-3" rows="3" placeholder="Enter your prompt"></textarea>
          </div>

          <div class="flex justify-end">
  
            <button id="generate-button" class="bg-blue-500 hover:bg-blue-600 text-white font-medium py-2 px-4 rounded-lg">Generate Image</button>

          </div>
        </div>

        <div class="bg-white p-4 rounded-lg shadow-md mt-4">           
          <h2 class="text-lg font-bold mb-2">Generated Image</h2>
          <div id="error-message" class="text-red-500 mb-4"></div>    
          <div id="generated-image" class="w-full h-[400px] bg-gray-200 rounded-lg flex items-center justify-center">
            <p class="text-gray-500">Your generated image will appear here.</p>
          </div>
        </div>
      </div>
    </div>
  </div>

    
<!-- Login Modal -->
<div id="login-modal" class="fixed z-10 inset-0 overflow-y-auto hidden">
  <div class="flex items-center justify-center min-h-screen">
    <div class="bg-white p-6 rounded-lg shadow-md relative">
        
     <div id="login-error-message" class="text-red-500 mb-4"></div>
    
        
      <button id="close-login-modal" class="absolute top-2 right-2 text-gray-500 hover:text-gray-700">&times;</button>
      <h2 class="text-lg font-bold mb-4">Login</h2>
      <form id="login-form">
        <div class="mb-4">
          <label for="login-username" class="block font-medium mb-1">Username</label>
          <input type="text" id="login-username" name="username" class="w-full bg-gray-200 rounded-lg py-2 px-3">
        </div>
        <div class="mb-4">
          <label for="login-password" class="block font-medium mb-1">Password</label>
          <input type="password" id="login-password" name="password" class="w-full bg-gray-200 rounded-lg py-2 px-3">
        </div>
        <div class="flex justify-end">
          <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white font-medium py-2 px-4 rounded-lg">Login</button>
        </div>
      </form>
    </div>
  </div>
</div>

    
    
<!-- Register Modal -->
<div id="register-modal" class="fixed z-10 inset-0 overflow-y-auto hidden">
  <div class="flex items-center justify-center min-h-screen">
    <div class="bg-white p-6 rounded-lg shadow-md relative">
      <button id="close-register-modal" class="absolute top-2 right-2 text-gray-500 hover:text-gray-700">&times;</button>
      <h2 class="text-lg font-bold mb-4">Register</h2>
      <form id="register-form">
        <div class="mb-4">
          <label for="register-username" class="block font-medium mb-1">Username</label>
          <input type="text" id="register-username" name="username" class="w-full bg-gray-200 rounded-lg py-2 px-3">
        </div>
        <div class="mb-4">
          <label for="register-email" class="block font-medium mb-1">Email</label>
          <input type="email" id="register-email" name="email" class="w-full bg-gray-200 rounded-lg py-2 px-3">
        </div>
        <div class="mb-4">
          <label for="register-password" class="block font-medium mb-1">Password</label>
          <input type="password" id="register-password" name="password" class="w-full bg-gray-200 rounded-lg py-2 px-3">
        </div>
        <div class="flex justify-end">
          <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white font-medium py-2 px-4 rounded-lg">Register</button>
        </div>
      </form>
    </div>
  </div>
</div>



  <script src="/script.js"></script>
</body>
</html>