<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Images</title>
  <style>
    table {
      width: 100%;
      border-collapse: collapse;
    }
    th, td {
      padding: 8px;
      border: 1px solid #ddd;
      text-align: left;
    }
    th {
      background-color: #f2f2f2;
    }
    .thumbnails img {
      width: 100px;
      height: 100px;
      object-fit: cover;
    }
  </style>
</head>
<body>
  <h1>Images by <%= user.username %></h1>
  <p>Email: <%= user.email %></p>

  <h2>Images</h2>
  <table border="1">
    <tr>
      <th>Thumbnail</th>
      <th>Prompt</th>
      <th>Visibility</th>
      <th>Created At</th>
    </tr>
    <% images.forEach(image => { %>
      <tr>
        <td class="thumbnails">
          <img src="<%= image.thumbnailUrl %>" alt="Thumbnail">
        </td>
        <td><%= image.prompt %></td>
        <td><%= image.isPublic ? 'Public' : 'Private' %></td>
        <td><%= image.createdAt.toLocaleString() %></td>
      </tr>
    <% }) %>
  </table>

  <a href="/admin">Back to Admin Dashboard</a>
</body>
</html>